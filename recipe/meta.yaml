{% set name = "aiodns" %}
{% set version = "3.1.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: fadb59304f4e38fd23b2f1953107df5cf29121d66c4e9c37a9bb13832c0c0513

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - python >=3.6
    - pip
  run:
    - python >=3.6
    - pycares >=4.0.0

test:
  imports:
    - aiodns
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/saghul/aiodns
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: Simple DNS resolver for asyncio
  description: |
    aiodns provides a simple way for doing asynchronous DNS resolutions using
    pycares.
  doc_url: https://github.com/saghul/aiodns
  dev_url: https://github.com/saghul/aiodns

extra:
  recipe-maintainers:
    - CurtLH
